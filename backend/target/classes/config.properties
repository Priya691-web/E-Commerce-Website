# ============================================================
# FashionStore Configuration
# ============================================================
# NOTE: This file is NOT read by the application at runtime.
# All configuration is loaded from environment variables.
#
# Docker/Production environment variables:
#   DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD
#   REDIS_HOST, REDIS_PORT, REDIS_PASSWORD, REDIS_ENABLED
#   CORS_ALLOWED_ORIGINS
#   CSP_ALLOWED_FRAME_ANCESTORS
#   FASHIONSTORE_PROFILE (dev/staging/prod)
#   FASHIONSTORE_SESSION_COOKIE_SECURE (true/false)
#
# See: .env.development, .env.staging, .env.production for full examples.
# ============================================================

# Legacy placeholders (not used at runtime - kept for reference only)
#redis.host=${REDIS_HOST}
#redis.port=${REDIS_PORT}
#redis.password=${REDIS_PASSWORD}
#redis.enabled=${REDIS_ENABLED}
#redis.timeout=2000
