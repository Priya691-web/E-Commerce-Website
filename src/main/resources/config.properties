# Redis Configuration
redis.host=localhost
redis.port=6379
redis.password=
redis.enabled=true
redis.timeout=2000

# Cache Configuration
cache.enabled=true
cache.local.fallback=true

# Application Configuration
app.env=development
